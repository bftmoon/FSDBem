mixin toggle(text)
  label.toggle(class=attributes.class)
    - delete attributes.class
    .toggle__box
      input(type='checkbox').toggle__input&attributes(attributes)
      .toggle__checker
    span.toggle__text=text
