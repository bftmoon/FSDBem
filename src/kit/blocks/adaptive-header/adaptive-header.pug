include ../header/header
include ../sidebar/sidebar

mixin adaptive-header({links, login, register, profile})
  +header({links, login, register, profile})
  +sidebar({links, login, register, profile})
